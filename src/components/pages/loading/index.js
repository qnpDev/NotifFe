import React from 'react';
import ReactLoading from 'react-loading'
const Loading = () => {
    return (
        <ReactLoading type='spinningBubbles' color='red' className='center-v-h'/>
    );
};

export default Loading;